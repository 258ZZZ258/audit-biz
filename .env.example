# 本地后端栈 compose 用。cp .env.example .env 后按需改。.env 已 gitignore,绝不入库。
AUDIT_BIZ_PG_PASSWORD=change-me-local

# 内网 Profile 变量示例。真实地址、账号和密钥只写入本地 .env 或部署平台。
IDAP_DB_URL=
IDAP_DB_USERNAME=
IDAP_DB_PASSWORD=
DUBBO_REGISTRY_ADDRESS=N/A
REDIS_PASSWORD=
REDIS_SENTINEL_MASTER=
REDIS_SENTINEL_NODES=
CA_AUTH_URL=
CA_AUTH_REDIRECT_URL=
CA_AUTH_REDIRECT_BACKUP_URL=
CA_APP_ID=
CA_CLIENT_SECRET=
CA_DOMAIN_NAME=
GENESIS_AUTH_ENABLED=false
